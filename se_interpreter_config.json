{
  "type": "interpreter-config",
  "configurations": [
    {
      "settings": [
        {
          "driverOptions": {
            "host": "localhost",
            "port": 4445
          },
          "browserOptions": {
            "browserName": "chrome",
            "username": "${SAUCE_LABS_USERNAME}",
            "accessKey": "${SAUCE_LABS_ACCESS_KEY}"
          }
        }
      ],
      "scripts": [
        "tests/selenium/philipp/acessa/SeleniumTestHostel"
      ]
    }
  ]
}
